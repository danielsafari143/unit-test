class Calculatore {
    add (first , second ){
        return first + second
    }
    substract (first , second ){
        return first - second
    }
    divide (first , second ){
        return first / second
    }
}

module.exports = Calculatore;